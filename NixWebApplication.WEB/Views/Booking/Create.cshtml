@model BookingModel

@{
    ViewData["Title"] = "Create";
}

<h1>Create</h1>

<h4>PriceToCategory</h4>
<hr />
<div class="row">
    <div class="col-md-4">
        <form asp-action="Create">
            <div asp-validation-summary="ModelOnly" class="text-danger"></div>
            <div class="form-group">
                <label asp-for="BookingGuest" class="control-label"></label>
                <select asp-for="BookingGuest.Id"
                        asp-items="ViewBag.GuestsForBookings" class=" form-control">
                    <option value="">-- Select Guest --</option>
                </select>
                <span asp-validation-for="BookingGuest.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="BookingRoom" class="control-label"></label>
                <select asp-for="BookingRoom.Id"
                        asp-items="ViewBag.RoomsForBookings" class=" form-control">
                    <option value="">-- Select Room --</option>
                </select>
                <span asp-validation-for="BookingRoom.Id" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="EnterDate" class="control-label"></label>
                <input asp-for="EnterDate" class="form-control" />
                <span asp-validation-for="EnterDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <label asp-for="LeaveDate" class="control-label"></label>
                <input asp-for="LeaveDate" class="form-control" />
                <span asp-validation-for="LeaveDate" class="text-danger"></span>
            </div>
            <div class="form-group">
                <input type="submit" value="Create" class="btn btn-primary" />
            </div>
        </form>
    </div>
</div>

<div>
    <a asp-action="Index">Back to List</a>
</div>

@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}
}
